C++ 单链表反转核心概念：
反转单链表不需要申请新的节点内存，只需改变现有节点中 next 指针的指向。
在遍历链表时，需要维护三个指针：
1. prev（前驱指针）：用于保存上一个节点，初始化为 nullptr。
2. curr（当前指针）：用于遍历链表，初始化为 head。
3. next_temp（后继指针）：用于临时保存 curr->next，防止断链。
边界条件测试：必须检查链表是否为空 (head == nullptr) 或仅有一个节点 (head->next == nullptr)。